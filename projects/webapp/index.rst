WebApp project
==============

This is the WebApp project, which includes various components such as API and authentication.
This file is only used if no component is selected via::

    --define=use_incl_bits=true --define=incl_webapp_api=true --define=incl_webapp_auth=true

and if the traceability flag is not set::

    --define=trace_only=true

.. toctree::
    :caption: Contents
    :glob:

    */docs/index
